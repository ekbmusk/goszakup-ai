# Бесплатный деплой на Render.com

## 🆓 Полностью бесплатный вариант

### Преимущества:
- ✅ Бесплатно навсегда (с ограничениями)
- ✅ Автодеплой из GitHub
- ✅ SSL сертификаты включены
- ✅ Простая настройка
- ✅ 750 часов работы в месяц

### Ограничения бесплатного tier:
- ⏱️ Засыпает после 15 минут неактивности
- 🐌 Первый запрос после сна ~30 секунд
- 💾 512MB RAM, 0.1 CPU
- 🌐 Один регион на выбор

---

## 📝 Инструкция по деплою:

### Шаг 1: Регистрация

1. Идите на https://render.com
2. **Sign Up with GitHub** (войдите через GitHub)
3. Дайте доступ к вашим репозиториям

### Шаг 2: Создание Web Service

1. На дашборде нажмите **"New +"** → **"Web Service"**

2. **Connect a repository**:
   - Найдите `ekbmusk/goszakup-ai`
   - Нажмите **"Connect"**

3. **Настройки сервиса**:
   ```
   Name: goszakup-ai
   Region: Frankfurt (ближе к КZ)
   Branch: main
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: uvicorn src.api.routes:app --host 0.0.0.0 --port $PORT --workers 1
   ```

4. **Plan**: Выберите **"Free"** (0$/месяц)

### Шаг 3: Environment Variables

Добавьте переменные окружения:

```
GOSZAKUP_TOKEN = [ваш токен]
FORCE_TRAIN = false
EXPORT_TRAIN_DATA = false
API_KEY = [создайте случайный ключ]
PYTHON_VERSION = 3.11.0
```

### Шаг 4: Deploy!

1. Нажмите **"Create Web Service"**
2. Деплой займёт 5-10 минут
3. Получите URL: `https://goszakup-ai.onrender.com`

---

## 🧪 Тестирование после деплоя

```bash
# Health check
curl https://goszakup-ai.onrender.com/api/health

# Список лотов
curl "https://goszakup-ai.onrender.com/api/lots?page=0&size=5"

# Анализ текста
curl -X POST https://goszakup-ai.onrender.com/api/analyze \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Требуется Apple MacBook Pro 16 inch",
    "budget": 5000000,
    "participants_count": 2,
    "deadline_days": 7
  }'
```

---

## ⚠️ Важные замечания

### Холодный старт
После 15 минут неактивности сервис "засыпает". Первый запрос разбудит его (~30 сек). Последующие запросы будут быстрыми.

**Решение**: Используйте cron-сервис для периодических пингов:
- https://cron-job.org (бесплатно)
- https://uptimerobot.com (бесплатно)

Пингуйте `/api/health` каждые 10 минут.

### Ограничения RAM
512MB может быть мало для CatBoost моделей. Если приложение падает с OOM:

1. Отключите CatBoost (используйте только Rule Engine + TF-IDF)
2. Уменьшите размер модели
3. Или апгрейдьте на Starter Plan ($7/мес) с 512MB → 1GB RAM

### Логи и мониторинг
- **Logs**: В дашборде Render → Events
- **Metrics**: CPU, Memory usage доступны в UI

---

## 💰 Сравнение с другими вариантами

| Платформа | Бесплатный план | Засыпает? | RAM | Стоимость платного |
|-----------|-----------------|-----------|-----|-------------------|
| **Render.com** | ✅ 750 ч/мес | Да (15 мин) | 512MB | $7/мес |
| **Railway.app** | $5 кредит/мес | Нет | 512MB | $5-20/мес |
| **Fly.io** | 3 VMs | Нет | 256MB | $0-10/мес |
| **DigitalOcean** | ❌ Нет | Нет | 1GB+ | $12+/мес |

**Вывод**: 
- Для **тестирования/демо** → Render.com (бесплатно)
- Для **production** → DigitalOcean или Railway ($12-24/мес)

---

## 🚀 Следующие шаги

1. Зарегистрируйтесь на https://render.com
2. Подключите GitHub репозиторий
3. Следуйте инструкциям выше
4. Через 10 минут ваше API будет онлайн!

---

## 🔧 Альтернатива: Railway.app

Если Render.com не подходит, попробуйте Railway.app:

```bash
# Установите Railway CLI (опционально)
npm install -g @railway/cli

# Или просто используйте веб-интерфейс:
# https://railway.app/new
```

Railway даёт $5/месяц бесплатно = ~500 часов работы одного сервиса.
