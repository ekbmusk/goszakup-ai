# GoszakupAI 🇰🇿

**AI-система анализа рисков в государственных закупках Республики Казахстан**

Система использует машинное обучение, NLP и анализ графов для автоматического выявления потенциально коррупционных практик, картельных сговоров и дискриминационных требований в тендерной документации.

---

## 🎯 Основные возможности

- **30+ индикаторов риска** на основе методологии Datanomix
- **ML-скоринг** (CatBoost + Isolation Forest)
- **Семантический поиск** дубликатов и плагиата ТЗ (LaBSE/TF-IDF)
- **Сетевой анализ** связей заказчик-поставщик (NetworkX)
- **GraphQL API клиент** с пагинацией и rate limiting
- **REST API** (FastAPI) для интеграции
- **Docker/Docker Compose** для быстрого развёртывания

---

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Docker (опционально)

### Локальная установка

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/goszakup-ai.git
cd goszakup-ai

# Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Настройте переменные окружения
cp env.example .env
# Отредактируйте .env и добавьте ваш GOSZAKUP_TOKEN (опционально)

# Запустите анализ
python main.py

# Или запустите FastAPI сервер
uvicorn src.api.routes:app --reload --port 8006
```

### Docker

```bash
# Запуск через Docker Compose
docker-compose up -d

# API будет доступен на http://localhost:8000
```

---

## 📚 Документация

- [IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md) - Обзор реализации
- [GRAPHQL_GUIDE.md](GRAPHQL_GUIDE.md) - GraphQL клиент и пагинация
- [SECURITY.md](SECURITY.md) - Безопасность и best practices

---

## 🏗️ Архитектура

```
┌─────────────────────┐
│  Goszakup API       │
│  (GraphQL / REST)   │
└──────────┬──────────┘
           │
    ┌──────▼──────┐
    │  Ingestion  │
    └──────┬──────┘
           │
    ┌──────▼──────────┐
    │ Preprocessing   │
    │ (NER, Features) │
    └──────┬──────────┘
           │
    ┌──────▼──────────┐
    │  Rule Engine    │
    │  (30+ правил)   │
    └──────┬──────────┘
           │
    ┌──────▼──────────┐
    │   ML Models     │
    │ CatBoost + ISO  │
    └──────┬──────────┘
           │
    ┌──────▼──────────┐
    │   Vectorizer    │
    │ (Similarity)    │
    └──────┬──────────┘
           │
    ┌──────▼──────────┐
    │Network Analysis │
    │   (NetworkX)    │
    └──────┬──────────┘
           │
    ┌──────▼──────────┐
    │   FastAPI       │
    │  REST Endpoint  │
    └─────────────────┘
```

---

## 📊 Примеры использования

### CLI Анализ

```bash
python main.py
```

### FastAPI Endpoints

```bash
# Проверка здоровья системы
curl http://localhost:8006/api/health

# Список лотов с фильтрацией
curl "http://localhost:8006/api/lots?risk_level=HIGH&page=0&size=10"

# Детальный анализ одного лота
curl http://localhost:8006/api/lot/LOT-2024-001

# Анализ произвольного текста
curl -X POST http://localhost:8006/api/analyze \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Требуется Apple MacBook Pro 16...",
    "budget": 5000000,
    "participants_count": 2,
    "deadline_days": 7
  }'
```

### Streamlit UI

```bash
streamlit run streamlit_app.py
```

---

## 🔒 Безопасность

### ⚠️ ВАЖНО: Перед деплоем

**НЕ ДЕЛАЙТЕ:**
- ❌ Не коммитьте файлы `.env` с реальными токенами
- ❌ Не публикуйте ваш `GOSZAKUP_TOKEN` в открытом доступе
- ❌ Не запускайте production без HTTPS
- ❌ Не отключайте rate limiting в production

**ДЕЛАЙТЕ:**
- ✅ Используйте сильные, уникальные токены
- ✅ Держите зависимости обновлёнными
- ✅ Включайте rate limiting
- ✅ Используйте HTTPS (Let's Encrypt бесплатно)
- ✅ Мониторьте логи на подозрительную активность

См. [SECURITY.md](SECURITY.md) для деталей.

---

## 🛠️ Технологический стек

- **Backend**: FastAPI, Uvicorn, Pydantic, HTTPX
- **ML**: CatBoost, scikit-learn, Sentence-Transformers (LaBSE)
- **NLP**: TF-IDF, регулярные выражения, Natasha (опционально)
- **Графы**: NetworkX
- **Data**: NumPy, Pandas
- **DevOps**: Docker, Docker Compose
- **Database**: PostgreSQL + pgvector (опционально)

---

## 📦 Структура проекта

```
goszakup-ai/
├── src/
│   ├── api/              # FastAPI REST API
│   ├── ingestion/        # Загрузка данных (GraphQL/REST)
│   ├── preprocessing/    # NER, очистка текста, feature engineering
│   ├── model/            # ML модели и Rule Engine
│   └── utils/            # Конфигурация и утилиты
├── data/
│   ├── raw/              # Исходные данные
│   ├── processed/        # Обработанные данные
│   └── models/           # Сохранённые модели
├── tests/                # Тесты
├── main.py               # CLI точка входа
├── streamlit_app.py      # Streamlit UI
├── requirements.txt      # Python зависимости
├── Dockerfile            # Docker образ
└── docker-compose.yml    # Оркестрация
```

---

## 🎓 Индикаторы риска

Система проверяет **30+ правил** в 4 категориях:

### Дискриминация (R01-R08)
- Брендирование без эквивалентов
- Проприетарные технологии
- Каталожные номера
- Избыточная точность характеристик

### Сговор (R09-R14)
- Короткие сроки подачи
- Малое количество участников
- Повторяющиеся победители
- Сегментирование контрактов

### Коррупция (R15-R22)
- Признаки люксовых товаров
- Необоснованно высокая цена
- Частые пары заказчик-поставщик

### Процедурные нарушения (R23-R30)
- Некорректные критерии оценки
- Отсутствие прозрачности

---

## 🚢 Деплой

### DigitalOcean Droplet

```bash
# На сервере (Ubuntu 22.04)
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

git clone https://github.com/your-username/goszakup-ai.git
cd goszakup-ai

# Создайте .env с реальными значениями
nano .env

# Запустите
docker-compose up -d

# Настройте Nginx + SSL
apt install nginx certbot python3-certbot-nginx
# ... (см. документацию)
```

### DigitalOcean App Platform

1. Подключите GitHub репозиторий
2. Настройте переменные окружения
3. Deploy автоматически при push

---

## 🤝 Вклад в проект

Contributions are welcome! Please read [SECURITY.md](SECURITY.md) before submitting.

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

---

## 📮 Контакты

- GitHub Issues: [Issues](https://github.com/your-username/goszakup-ai/issues)
- Documentation: [Wiki](https://github.com/your-username/goszakup-ai/wiki)

---

## 🌟 Acknowledgments

- Методология индикаторов риска основана на исследованиях [Datanomix](https://datanomix.pro)
- Используется открытая платформа данных [goszakup.gov.kz](https://goszakup.gov.kz)

---

**⚖️ Дисклеймер**: Эта система предоставляет аналитические индикаторы и не является юридически обязывающим инструментом. Все решения должны приниматься уполномоченными органами на основе всестороннего анализа.
